/*
 * config service
 * ---------------
 * 
 * will hold different configurations regarding promo 
 * pages client side behavior.
 * 
 * for example, if auto focus on phone input atc.
 * 
 */

var config = {
	
	/*
	 * determines if auto focus should be on phone input.
	 * will be turrned of on mobile pages, to avoid keyoard 
	 * auto oppening
	 */
	auto_phone_input_focus:true
	
};
